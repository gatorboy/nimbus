{
    "name": "demo-www",
    "version": "2.0.0-beta3",
    "description": "A demo hybrid app for nimbus",
    "license": "BSD-3-Clause",
    "private": true,
    "main": "src/index.js",
    "scripts": {
        "prepare": "npm run build",
        "build": "cross-env rollup -c ./rollup.config.js",
        "clean": "rm -rf ./dist",
        "serve": "cross-env rollup -c ./rollup.config.js --watch",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "@nimbus-js/api": "file:../@nimbus-js/api",
        "@nimbus-js/runtime": "file:../@nimbus-js/runtime"
    }
}
