{
    "name": "test-www",
    "version": "2.0.0-beta3",
    "description": "",
    "license": "BSD-3-Clause",
    "private": true,
    "browser": "dist/test-www/bundle.js",
    "scripts": {
        "prepare": "npm run build",
        "build": "cross-env rollup -c ./rollup.config.js",
        "clean": "rm -rf ./dist",
        "serve": "cross-env rollup -c ./rollup.config.js --watch"
    },
    "dependencies": {
        "@nimbus-js/api": "file:../@nimbus-js/api"
    }
}
